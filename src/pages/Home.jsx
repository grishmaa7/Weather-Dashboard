const Home = () => {
    return (
        <div style={{ padding: "2rem" }}>
            <h1>Welcome to the Weather Dashboard</h1>
            <p>Select a city from the Cities page to see its current weather.</p>
        </div>
    );
};

export default Home;
